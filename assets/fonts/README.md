Please add a Vietnamese-capable TTF file here named:

  NotoSans-Regular.ttf

Recommended source: https://fonts.google.com/specimen/Noto+Sans

Place the file at `assets/fonts/NotoSans-Regular.ttf` so the app's PDF printer can load it at runtime.

After adding the font, run:

  flutter pub get

Then rebuild the app.